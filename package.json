{
  "name": "personal-portifolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "studio": "cd studio && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "dev:cms": "concurrently \"npm run client\" \"npm run studio\"",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../studio && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "react-icons": "^5.5.0"
  }
}
