# Copy to `.env` and fill in. Sanity only exposes vars prefixed SANITY_STUDIO_.
# Find your project ID at https://www.sanity.io/manage
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Hostname for `npm run deploy` -> https://<host>.sanity.studio
SANITY_STUDIO_HOST=
